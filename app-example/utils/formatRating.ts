/**
 * Форматує рейтинг до одного знаку після коми.
 * 
 * Якщо рейтинг ціле число (наприклад, 5), повертає "5.0".
 * Якщо дробове (наприклад, 4.9), залишає "4.9".
 */

export const formatRating = (rating: number): string => {
    return rating.toFixed(1);
  };